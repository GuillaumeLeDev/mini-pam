# Clé API pour authentifier les appels au vault (changer en production)
VAULT_API_KEY=dev-api-key-change-me

# Clé Fernet pour le chiffrement des secrets en base
# Générer avec : python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=

# Clé API pour authentifier les appels au bastion
BASTION_API_KEY=dev-bastion-key-change-me

# Utilisateur PAM connecté au portail (pas d'auth réelle dans le lab)
PORTAL_USER=guillaume
